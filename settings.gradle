dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "NumberPickerPreferenceCompat"

include ':app', ':library'

//include ':android-numberpickercompat'
//project(':android-numberpickercompat').projectDir = new File(rootDir, 'android-numberpickercompat/library')
